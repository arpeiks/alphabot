import React from "react";

import { Root } from "./chat.styles";
import Textbox from "../textbox/textbox";

const Chat = () => {
  return (
    <Root>
      <div>chat</div>
      <Textbox />
    </Root>
  );
};

export default Chat;
